node_modules/
dist/
.astro/
.env
.env.production
.DS_Store
.vscode/
.idea/
*.log

# Source images dropped at the project root (originals for the hero).
# The optimized variants live under public/landing/.
/gimbo*.png
/个人网站图.png
/*.psd
/*.ai

.vercel
.claude/

/首页图片*.png
# Book cover alternates dropped at root (e.g. 三体版.png, 阿凡达版.png).
# Official cover variants live under public/.
/*版.png

# AI image-tool staging drop folder. Sources for the section heroes are
# duplicated into public/landing/*-hero.png (which IS tracked).
/public/photos produced by ai/
